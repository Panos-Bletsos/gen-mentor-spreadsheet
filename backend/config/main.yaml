# Hydra entrypoint config that composes the default application settings
defaults:
  - default
  - _self_

debug: true
log_level: DEBUG

# Optionally, override server settings here
server:
  host: 127.0.0.1
  port: 5000

llm:
  provider: openai
  model_name: gpt-4o
  base_url: null
